{
  "name": "qrv-marketing-site",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "description": "Public marketing website and navigation hub for the QR-V Global Verification Network at qrv.network.",
  "scripts": {
    "dev": "node server.js",
    "check": "node scripts/check.mjs",
    "build": "node scripts/build.mjs",
    "start": "node server.js",
    "validate:prod": "npm run check && npm run build"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=20"
  }
}
